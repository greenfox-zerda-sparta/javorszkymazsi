#include <iostream>
#include <string>

using namespace std;

int main() {
	int g1 = 123;
	int g2 = 345;
	// tell if g1 is bigger than g2

	if ( g1 > g2 ) {
		cout << g1 << " is bigger than " << g2 << "." << endl;
	} else {
		cout << g1 << " is not bigger than " << g2 << "." << endl;
	}

	return 0;
}
